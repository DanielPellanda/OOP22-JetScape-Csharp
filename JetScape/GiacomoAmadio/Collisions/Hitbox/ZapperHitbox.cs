using System.Drawing;
using System.Collections.Generic;

namespace JetScape.Collisions.Hitbox
{
    /*
    public class ZapperHitbox : HitboxInstance 
    { 
        public ZapperHitbox(ZapperBase base1, ZapperBase base2,
             ISet<ZapperRay> rays, Point startingPos) :base (startingPos)
        {
            foreach (ZapperRay ray in rays)
            {
                addHitbox(ray.GetHitbox());
            }
            addHitbox(base1.GetHitbox());
            addHitbox(base2.GetHitbox());
        }
    } */
}
